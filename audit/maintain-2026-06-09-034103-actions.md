All done. Here's the summary:

## Dependency Update Summary

**Updated packages** (3, all patch-level bumps within the `typescript-eslint` ecosystem):
- `@typescript-eslint/eslint-plugin`: 8.60.1 → 8.61.0
- `@typescript-eslint/parser`: 8.60.1 → 8.61.0
- `typescript-eslint`: 8.60.1 → 8.61.0

**Quality gates** — all passed ✅:
- `npm run lint` — zero warnings
- `npm run format:check` — all files use Prettier code style
- `npm test` — 715 tests across 38 suites, all passing
- `npm run build` — TypeScript compiles cleanly
- `npm audit --omit=dev --audit-level=moderate` — 0 vulnerabilities

Committed and pushed to `main` as `chore: update typescript-eslint to 8.61.0`.